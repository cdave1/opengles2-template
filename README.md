This is an iPad app project file intended to be used as a quick and dirty starting point for mucking around with OpenGL ES 2 shaders.  

The project also contains a few basic necessities, such as vector and matrix types and several matrix manipulation functions.